# 변환 생성자
**변환 생성자**: 기본 타입으로부터 객체를 만드는 생성자이며 인수를 하나만 취한다  
> 변환 생성자는 반드시 인수를 하나만 취해야 하며 둘 이상을 취할 경우 변환 생성자가 아니다.  
> explicit로 지정된 생성자는 암시적인 형 변환에 사용할 수 없도록 금지된다.  
> 변환 생성자는 필요한만큼 정의할 수 있다.  

<pre>
#include <iostream>

class Time
{
private:
    int hour, min, sec;

public:
    Time() {}

    Time(int abssec)
    {
        hour = abssec / 3600;
        min = (abssec / 60) % 60;
        sec = abssec % 60;
    }

    void OutTime()
    {
        printf("현재 시간은 %d:%d:%d입니다.\n", hour, min, sec);
    }
};

void main()
{
    Time Now(3723);
    Now.OutTime();

    Now=6823;
    Now.OutTime();
}
</pre>
